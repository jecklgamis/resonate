**What does this change and why?**

**Related issue** (if this implements/fixes a discussed proposal):

**Checklist**

- [ ] `make check` passes locally (fmt-check + vet + test)
- [ ] Added/updated tests for the change
- [ ] If a flag/YAML field was added/removed/renamed, ran `make docs` and
      committed the regenerated `docs/cli/*.md`
- [ ] If user-facing behavior changed, updated the relevant `docs/*.md`
      page (`docs/` is the source of truth — see CLAUDE.md)
- [ ] Kept this PR focused on one change (see CONTRIBUTING.md)
